#include <stdio.h>

/**
 * main-A program that prints single digit numbers of base 10 starting from 0
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)

	{
		printf("%d", i);
	}
	return (0);
}
